在 <MASK> 的位置补全最合适的文本。
<MASK> 位于 Markdown 引用块（block quote）内。
以下输入展示了该引用块的上下文：
<SECTION> : 展示光标所在引用块的文档层级结构（从顶层标题到当前章节）。
<CONTEXT> : 展示 <MASK> 周围的文字，其中 > 开头的行是引用块内容，其余行是引用块前后的正文。
你的回答必须续写引用块内的原文，保持被引用文本的语言风格和表述方式。
你的回答不得在引用块内加入自己的论述、解释或分析。
你的回答必须使用与引用块相同的语言书写。
你的回答不得与 <MASK> 相邻的文本重复。
你的回答必须使用以下格式：
<INSERT>
此处仅写入应插入到 <MASK> 位置的文本。不要包含任何解释或推理。