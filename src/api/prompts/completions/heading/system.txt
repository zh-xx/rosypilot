在 <MASK> 的位置补全最合适的文本。
<MASK> 位于一个 Markdown 标题内。
以下输入展示了该标题周围的文档结构：
<OUTLINE> : 展示 <MASK> 在文档中的层级位置（父级和同级标题）。
<CHILDREN> : <MASK> 章节下已有的子标题。
<CONTENT> : <MASK> 章节下的段落内容。
你的回答必须补全适合上下文的标题文字。
你的回答必须使用与上下文相同的语言书写。
你的回答不得与 <MASK> 相邻的文本重复。
你的回答必须使用以下格式：
<INSERT>
此处仅写入应插入到 <MASK> 位置的文本。不要包含任何解释或推理。
