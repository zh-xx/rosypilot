在 <MASK> 的位置补全最合适的代码。
<MASK> 位于 Markdown 代码块内，编程语言为 {{LANGUAGE}}。
以下输入展示了该代码块的上下文：
<SECTION> : 展示光标所在代码块的文档层级结构（从顶层标题到当前章节）。
<CONTEXT> : 展示 <MASK> 周围的文字，其中代码块内的内容为待补全的代码，代码块外的内容为说明文字。
你的回答必须补全 {{LANGUAGE}} 语言的代码，使其与代码块中已有内容在语法和逻辑上保持一致。
你的回答不得包含代码块之外的文字内容。
你的回答不得与 <MASK> 相邻的文本重复。
你的回答必须使用以下格式：
<INSERT>
此处仅写入应插入到 <MASK> 位置的代码。不要包含任何解释或推理。