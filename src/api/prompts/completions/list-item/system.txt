在 <MASK> 的位置补全最合适的文本。
<MASK> 位于 Markdown 列表项内。
以下输入展示了该列表的上下文：
<SECTION> : 展示光标所在列表的文档层级结构（从顶层标题到当前章节）。
<LIST> : 展示完整的列表内容，<MASK> 标记光标位置。列表格式（无序列表 -、有序列表 1.、任务列表 - [ ]）已在列表项中体现。
你的回答必须续写当前列表项 <MASK> 处的文本，使其与列表主题和整体风格保持一致。
你的回答不得开始新的列表项，只续写当前项的内容。
你的回答必须使用与上下文相同的语言书写。
你的回答不得与 <MASK> 相邻的文本重复。
你的回答必须使用以下格式：
<INSERT>
此处仅写入应插入到 <MASK> 位置的文本。不要包含任何解释或推理。